/* ==============================================================
   APEX RUN 2026 — ENHANCEMENT LAYER STYLES
   Pairs 1:1 with enhance.js. Load AFTER style.css:

     <link rel="stylesheet" href="style.css">
     <link rel="stylesheet" href="enhance.css">
     ...
     <script src="script.js"></script>
     <script src="enhance.js"></script>

   Uses the tokens already defined in style.css (--cyan, --gold,
   --dawn, --font-display, --ease-cinematic) so nothing new is
   introduced to the palette.
============================================================== */

:root {
  --good: #00e676;
  --bad: #ff5a5a;
  --warn: var(--gold);
  --panel: linear-gradient(160deg, rgba(0, 29, 61, 0.92), rgba(0, 8, 20, 0.94));
  --z-progress: 1100;
  --z-float: 1200;
  --z-toast: 10001;
}

/* ==============================================================
   SKIP LINK — first tab stop
============================================================== */
.skip-link {
  position: fixed;
  top: 10px;
  left: 50%;
  z-index: 10002;
  padding: 10px 20px;
  border-radius: 0 0 12px 12px;
  font-size: 0.86rem;
  letter-spacing: 0.5px;
  color: #000814;
  background: var(--cyan);
  transform: translate(-50%, -140%);
  transition: transform 0.3s var(--ease-cinematic);
}

.skip-link:focus-visible {
  transform: translate(-50%, 0);
  outline: 2px solid white;
  outline-offset: 2px;
}

/* ==============================================================
   SECTION FRAMES — bracketed border that draws on arrival
============================================================== */
.has-sec-frame { position: relative; }

.sec-frame {
  position: absolute;
  inset: clamp(6px, 1.8vw, 20px);
  z-index: 0;
  pointer-events: none;
}

.sec-frame-edge {
  position: absolute;
  opacity: 0.45;
  background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.55), transparent);
  transition: transform 0.9s var(--ease-cinematic);
}

.sec-frame-edge.top,
.sec-frame-edge.bottom {
  left: 0;
  right: 0;
  height: 1px;
  transform: scaleX(0);
}

.sec-frame-edge.left,
.sec-frame-edge.right {
  top: 0;
  bottom: 0;
  width: 1px;
  background: linear-gradient(180deg, transparent, rgba(255, 183, 3, 0.4), transparent);
  transform: scaleY(0);
}

.sec-frame-edge.top    { top: 0;    transform-origin: left; }
.sec-frame-edge.bottom { bottom: 0; transform-origin: right; }
.sec-frame-edge.left   { left: 0;   transform-origin: top; }
.sec-frame-edge.right  { right: 0;  transform-origin: bottom; }

.sec-frame.is-drawn .sec-frame-edge.top,
.sec-frame.is-drawn .sec-frame-edge.bottom { transform: scaleX(1); }

.sec-frame.is-drawn .sec-frame-edge.left,
.sec-frame.is-drawn .sec-frame-edge.right { transform: scaleY(1); }

.sec-frame.is-drawn .sec-frame-edge.bottom { transition-delay: 0.1s; }
.sec-frame.is-drawn .sec-frame-edge.left   { transition-delay: 0.2s; }
.sec-frame.is-drawn .sec-frame-edge.right  { transition-delay: 0.28s; }

.sec-frame-corner {
  position: absolute;
  width: 16px;
  height: 16px;
  opacity: 0;
  transition: opacity 0.5s ease 0.6s;
}

.sec-frame-corner.tl { top: -1px;    left: -1px;  border-top: 2px solid var(--cyan); border-left: 2px solid var(--cyan); border-radius: 6px 0 0 0; }
.sec-frame-corner.tr { top: -1px;    right: -1px; border-top: 2px solid var(--cyan); border-right: 2px solid var(--cyan); border-radius: 0 6px 0 0; }
.sec-frame-corner.bl { bottom: -1px; left: -1px;  border-bottom: 2px solid var(--gold); border-left: 2px solid var(--gold); border-radius: 0 0 0 6px; }
.sec-frame-corner.br { bottom: -1px; right: -1px; border-bottom: 2px solid var(--gold); border-right: 2px solid var(--gold); border-radius: 0 0 6px 0; }

.sec-frame.is-drawn .sec-frame-corner { opacity: 0.8; }

.sec-frame-spark {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  opacity: 0;
  background: linear-gradient(90deg, transparent, #fff, var(--cyan), transparent);
  background-size: 24% 100%;
  background-repeat: no-repeat;
  background-position: -30% 0;
  filter: blur(0.4px);
}

.sec-frame.is-drawn .sec-frame-spark {
  animation: secSpark 1.5s var(--ease-cinematic) 0.35s both;
}

@keyframes secSpark {
  0%   { opacity: 0; background-position: -30% 0; }
  18%  { opacity: 1; }
  100% { opacity: 0; background-position: 130% 0; }
}

/* ==============================================================
   SCROLL PROGRESS
============================================================== */
.scroll-progress {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 3px;
  z-index: var(--z-progress);
  transform: scaleX(0);
  transform-origin: left;
  background: linear-gradient(90deg, var(--cyan), var(--gold), var(--dawn));
  box-shadow: 0 0 12px rgba(0, 255, 255, 0.5);
  pointer-events: none;
}

/* ==============================================================
   TOASTS
============================================================== */
.toast-stack {
  position: fixed;
  right: 96px;
  bottom: 24px;
  z-index: var(--z-toast);
  display: flex;
  flex-direction: column;
  gap: 10px;
  width: min(340px, calc(100vw - 32px));
  pointer-events: none;
}

.toast {
  display: grid;
  grid-template-columns: 26px 1fr 26px;
  align-items: center;
  gap: 12px;
  padding: 13px 14px;
  border-radius: 14px;
  font-size: 0.88rem;
  line-height: 1.45;
  color: rgba(255, 255, 255, 0.95);
  background: var(--panel);
  border: 1px solid var(--glass-border);
  border-left: 3px solid var(--cyan);
  box-shadow: 0 18px 45px rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  pointer-events: auto;
  animation: toastIn 0.35s var(--ease-cinematic);
}

.toast[data-tone="good"] { border-left-color: var(--good); }
.toast[data-tone="bad"]  { border-left-color: var(--bad); }
.toast[data-tone="warn"] { border-left-color: var(--warn); }

@keyframes toastIn {
  from { opacity: 0; transform: translateY(14px) scale(0.97); }
  to   { opacity: 1; transform: none; }
}

.toast.is-leaving {
  opacity: 0;
  transform: translateX(20px);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.toast-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 26px;
  height: 26px;
  border-radius: 50%;
  font-size: 0.82rem;
  font-weight: 800;
  color: #000814;
  background: var(--cyan);
}

.toast[data-tone="good"] .toast-icon { background: var(--good); }
.toast[data-tone="bad"]  .toast-icon { background: var(--bad); color: white; }
.toast[data-tone="warn"] .toast-icon { background: var(--warn); }

.toast-close {
  width: 26px;
  height: 26px;
  border: none;
  border-radius: 50%;
  font-size: 1.05rem;
  line-height: 1;
  color: rgba(255, 255, 255, 0.6);
  background: rgba(255, 255, 255, 0.08);
  cursor: pointer;
  transition: background 0.25s ease, color 0.25s ease;
}

.toast-close:hover {
  background: rgba(255, 255, 255, 0.18);
  color: white;
}

/* ==============================================================
   BACK TO TOP
============================================================== */
.to-top {
  position: fixed;
  right: 24px;
  bottom: 24px;
  z-index: var(--z-float);
  width: 52px;
  height: 52px;
  display: grid;
  place-items: center;
  border: 1px solid rgba(0, 255, 255, 0.28);
  border-radius: 50%;
  background: rgba(0, 8, 20, 0.78);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  cursor: pointer;
  opacity: 0;
  visibility: hidden;
  transform: translateY(14px);
  transition: opacity 0.35s ease, transform 0.35s var(--ease-cinematic), visibility 0.35s;
}

.to-top.is-shown {
  opacity: 1;
  visibility: visible;
  transform: none;
}

.to-top:hover {
  border-color: var(--cyan);
  box-shadow: 0 0 22px rgba(0, 255, 255, 0.3);
}

.to-top svg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  transform: rotate(-90deg);
}

.to-top circle {
  fill: none;
  stroke-width: 2.5;
  stroke-linecap: round;
}

.to-top .track { stroke: rgba(255, 255, 255, 0.12); }

.to-top .bar {
  stroke: var(--cyan);
  transition: stroke-dashoffset 0.15s linear;
}

.to-top span {
  position: relative;
  font-size: 1.05rem;
  color: var(--cyan);
  line-height: 1;
}

/* ==============================================================
   STICKY REGISTER BAR (phones only)
============================================================== */
.sticky-cta {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: var(--z-float);
  display: none;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 12px 16px calc(12px + env(safe-area-inset-bottom));
  background: var(--panel);
  border-top: 1px solid rgba(0, 255, 255, 0.2);
  box-shadow: 0 -14px 40px rgba(0, 0, 0, 0.5);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  transform: translateY(115%);
  transition: transform 0.45s var(--ease-cinematic);
}

.sticky-cta.is-shown { transform: none; }

.sticky-cta-info {
  display: flex;
  flex-direction: column;
  gap: 1px;
  min-width: 0;
}

.sticky-cta-info strong {
  font-family: var(--font-display);
  font-size: 1.05rem;
  letter-spacing: 1.5px;
  color: var(--cyan);
}

.sticky-cta-info span {
  font-size: 0.72rem;
  opacity: 0.72;
  white-space: nowrap;
}

.sticky-cta-btn {
  flex-shrink: 0;
  padding: 11px 24px;
  border-radius: 30px;
  font-family: var(--font-display);
  font-size: 1rem;
  letter-spacing: 1.5px;
  color: black;
  background: linear-gradient(45deg, var(--gold), var(--orange));
  box-shadow: 0 8px 22px rgba(255, 140, 0, 0.32);
}

@media (max-width: 780px) {
  .sticky-cta { display: flex; }
  .to-top { bottom: calc(86px + env(safe-area-inset-bottom)); right: 16px; }
  .toast-stack {
    right: 12px;
    left: 12px;
    width: auto;
    bottom: calc(86px + env(safe-area-inset-bottom));
  }
}

/* Sticky bar overlaps the footer; give the page room to breathe. */
@media (max-width: 780px) {
  footer { padding-bottom: 92px; }
}

/* ==============================================================
   HERO STATUS PILL — countdown, then result
============================================================== */
.event-status {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  margin-top: 12px;
  padding: 8px 18px;
  border-radius: 30px;
  font-size: clamp(0.76rem, 1.9vw, 0.88rem);
  letter-spacing: 0.4px;
  color: rgba(255, 255, 255, 0.92);
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.14);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.event-status-dot {
  width: 8px;
  height: 8px;
  flex-shrink: 0;
  border-radius: 50%;
  background: var(--cyan);
  box-shadow: 0 0 10px currentColor;
}

.event-status[data-state="upcoming"] { border-color: rgba(0, 255, 255, 0.3); }

.event-status[data-state="live"] {
  border-color: rgba(255, 107, 74, 0.5);
  color: white;
}

.event-status[data-state="live"] .event-status-dot {
  background: var(--dawn);
  animation: statusBlink 1.2s ease-in-out infinite;
}

.event-status[data-state="past"] {
  opacity: 0.75;
  border-color: rgba(255, 255, 255, 0.12);
}

.event-status[data-state="past"] .event-status-dot {
  background: rgba(255, 255, 255, 0.4);
  box-shadow: none;
}

@keyframes statusBlink {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%      { opacity: 0.4; transform: scale(0.8); }
}

.countdown-units {
  display: inline-flex;
  gap: 5px;
  margin-left: 6px;
  vertical-align: middle;
}

.countdown-units b {
  min-width: 40px;
  padding: 3px 7px;
  border-radius: 7px;
  font-family: var(--font-display);
  font-size: 0.92rem;
  font-weight: 400;
  letter-spacing: 1px;
  text-align: center;
  color: var(--gold);
  background: rgba(0, 0, 0, 0.32);
  border: 1px solid rgba(255, 183, 3, 0.18);
  font-variant-numeric: tabular-nums;
}

/* ==============================================================
   POINTER SPOTLIGHT ON CARDS
============================================================== */
.contact-box p { position: relative; }

.card-spot {
  position: absolute;
  inset: 0;
  z-index: -1;
  border-radius: inherit;
  opacity: 0;
  pointer-events: none;
  background: radial-gradient(
    260px circle at var(--mx, 50%) var(--my, 50%),
    rgba(0, 255, 255, 0.16),
    transparent 62%
  );
  transition: opacity 0.35s ease;
}

.event-card:hover .card-spot,
.contact-box p:hover .card-spot { opacity: 1; }

.contact-box p .card-spot {
  background: radial-gradient(
    240px circle at var(--mx, 50%) var(--my, 50%),
    rgba(255, 215, 0, 0.16),
    transparent 62%
  );
}

/* ==============================================================
   MEDIA LOADING SHIMMER
============================================================== */
.media-skeleton {
  position: absolute;
  inset: 0;
  z-index: 5;
  border-radius: inherit;
  background:
    linear-gradient(100deg, transparent 30%, rgba(255, 255, 255, 0.09) 50%, transparent 70%),
    rgba(255, 255, 255, 0.045);
  background-size: 220% 100%, 100% 100%;
  animation: skeletonSweep 1.4s linear infinite;
  transition: opacity 0.45s ease, visibility 0.45s;
  pointer-events: none;
}

@keyframes skeletonSweep {
  from { background-position: 140% 0, 0 0; }
  to   { background-position: -60% 0, 0 0; }
}

.media-ready .media-skeleton {
  opacity: 0;
  visibility: hidden;
  animation: none;
}

/* ==============================================================
   LIGHTBOX NAVIGATION
============================================================== */
.lb-nav {
  position: absolute;
  top: 50%;
  z-index: 3;
  width: 48px;
  height: 48px;
  display: grid;
  place-items: center;
  border: 1px solid rgba(255, 255, 255, 0.16);
  border-radius: 50%;
  font-size: 1.9rem;
  line-height: 1;
  color: white;
  background: rgba(255, 255, 255, 0.09);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  cursor: pointer;
  transform: translateY(-50%);
  transition: background 0.25s ease, border-color 0.25s ease, transform 0.25s ease;
}

.lb-prev { left: clamp(10px, 3vw, 32px); }
.lb-next { right: clamp(10px, 3vw, 32px); }

.lb-nav:hover {
  background: rgba(0, 255, 255, 0.18);
  border-color: var(--cyan);
}

.lb-prev:hover { transform: translateY(-50%) translateX(-3px); }
.lb-next:hover { transform: translateY(-50%) translateX(3px); }

.lb-count {
  position: absolute;
  left: 50%;
  bottom: clamp(18px, 4vw, 34px);
  z-index: 3;
  padding: 6px 16px;
  border-radius: 20px;
  font-size: 0.8rem;
  letter-spacing: 1.5px;
  color: rgba(255, 255, 255, 0.8);
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.12);
  transform: translateX(-50%);
  font-variant-numeric: tabular-nums;
}

.lightbox-img.is-swapping {
  opacity: 0;
  transform: scale(0.97);
  transition: opacity 0.16s ease, transform 0.16s ease;
}

@media (max-width: 560px) {
  .lb-nav { width: 42px; height: 42px; font-size: 1.6rem; }
}

/* ==============================================================
   REGISTRATION FORM — progress rail
============================================================== */
.form-progress { margin-bottom: 26px; }

.form-progress-bar {
  height: 4px;
  border-radius: 4px;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.09);
}

.form-progress-fill {
  height: 100%;
  width: 0;
  border-radius: 4px;
  background: linear-gradient(90deg, var(--cyan), var(--gold));
  box-shadow: 0 0 12px rgba(0, 255, 255, 0.4);
  transition: width 0.5s var(--ease-cinematic);
}

.form-progress-text {
  margin-top: 9px;
  font-size: 0.8rem;
  letter-spacing: 0.4px;
  opacity: 0.72;
}

/* ==============================================================
   FIELD VALIDATION
============================================================== */
.field-msg {
  display: block;
  max-height: 0;
  overflow: hidden;
  font-size: 0.78rem;
  line-height: 1.5;
  opacity: 0;
  transition: max-height 0.3s ease, opacity 0.3s ease, margin 0.3s ease;
}

.field-msg.is-shown {
  max-height: 60px;
  margin: 2px 0 4px 2px;
  opacity: 1;
}

.field-msg[data-tone="bad"] { color: var(--bad); }

.registration-card input.field-invalid,
.registration-card input.field-invalid:focus {
  border-color: rgba(255, 90, 90, 0.7);
  box-shadow: 0 0 12px rgba(255, 90, 90, 0.22);
  background: rgba(255, 90, 90, 0.06);
}

.registration-card input.field-valid,
.registration-card input.field-valid:focus {
  border-color: rgba(0, 230, 118, 0.5);
  box-shadow: 0 0 12px rgba(0, 230, 118, 0.16);
}

.shake { animation: fieldShake 0.45s ease; }

@keyframes fieldShake {
  0%, 100%   { transform: translateX(0); }
  20%, 60%   { transform: translateX(-7px); }
  40%, 80%   { transform: translateX(7px); }
}

.form-group {
  position: relative;
  padding-left: 12px;
  border-left: 2px solid transparent;
  transition: border-color 0.35s ease, padding-left 0.35s var(--ease-cinematic);
}

.form-group.is-active {
  border-left-color: rgba(0, 255, 255, 0.45);
  padding-left: 16px;
}

/* ==============================================================
   MOTION PREFERENCES
============================================================== */
@media (prefers-reduced-motion: reduce) {
  .sec-frame-spark,
  .media-skeleton { animation: none; }

  .sec-frame-edge { transform: none; }
  .sec-frame-corner { opacity: 0.8; }
  .media-skeleton { opacity: 0; }
  .event-status[data-state="live"] .event-status-dot { animation: none; }
}
